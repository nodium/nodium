(function (window, undefined) {

'use strict';

var constants   = window.setNamespace('app.constants');

constants.NodeStatus = {
	ACCEPTED: 'accepted',
	DENIED: 'denied',
	PENDING: 'pending'
};

}(window));